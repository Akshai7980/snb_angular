import { NumbersOlyDirective } from './numbers-oly.directive';

describe('NumbersOlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersOlyDirective();
    expect(directive).toBeTruthy();
  });
});
