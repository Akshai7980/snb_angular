import { AmountUnformatPipePipe } from './amount-unformat-pipe.pipe';

describe('AmountUnformatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountUnformatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
